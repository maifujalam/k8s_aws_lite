apiVersion: v1
kind: Secret
metadata:
  name: istio-repository
  namespace: argo-cd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  name: istio-repository
  url: istio-release.storage.googleapis.com/charts
  type: helm
  enableOCI: "true"