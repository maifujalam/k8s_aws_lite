apiVersion: v1
kind: Secret
metadata:
  name: kiali-repository
  namespace: argo-cd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  name: kiali-repository
  url: kiali.org/helm-charts
  type: helm
  enableOCI: "true"