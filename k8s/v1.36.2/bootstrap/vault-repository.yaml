apiVersion: v1
kind: Secret
metadata:
  name: vault-repository
  namespace: argo-cd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  name: vault-repository
  url: helm.releases.hashicorp.com
  type: helm
  enableOCI: "true"