apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: elastic-gateway-tls-passthrough
  namespace: istio-ingress
spec:
  selector:
    istio: ingress-dev
  servers:
    - port:
        number: 443
        name: tls-passthrough
        protocol: HTTPS
      hosts:
        - elastic.maifuj.com
      tls:
        mode: PASSTHROUGH
